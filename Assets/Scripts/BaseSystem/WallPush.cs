using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class WallPush : MonoBehaviour
{
    private Rigidbody _rigidbody;
    public float pushPower = 10f;

    private void Awake()
    {
        _rigidbody = GetComponent<PlayersControl.PlayerMovement>(Rigidbody);
    }

    private void OnCollisionEnter(Collision collision)
    {
        if (collision.gameObject.name == "Player")
        {
            collision.gameObject.GetComponent<PlayersControl.PlayerMovement>()._rigidbody;
            _rigidbody.AddForce(0f, 0f, 1f * pushPower);
        }
    }
}*/
